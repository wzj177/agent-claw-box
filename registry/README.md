# AgentBox Registry
Agent Marketplace metadata — template listings, versions, checksums.

This directory will contain:
- `index.json` — catalog of all available agent templates
- Per-template metadata for the marketplace UI
